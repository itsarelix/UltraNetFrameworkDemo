@page "/SubOtp"



<div class="container mt-5">
    <div class="row">
        @foreach (var item in modules)
        {
            <div class="col-md-4 col-sm-6 mb-4">
                <div class="card module-card" @onclick="@(() => NavigateTo(item.Route))">
                    <div class="card-body text-center">
                        <div class="emoji-icon mb-3">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="@item.Class bi bi-lock-fill" viewBox="0 0 16 16">
                                <path fill-rule="evenodd" d=@item.Icon />
                            </svg>
                        </div>
                        <h5 class="SubTitle card-title">@item.Title</h5>
                        <p class="card-text">@item.Description</p>
                    </div>
                </div>
            </div>
        }
    </div>
</div>





@code {
  private List<ModuleCard> modules = new()
  {
    new() { Class = "IconSize OTPCustomStyle",Title = "Send Otp", Icon = "M15.854.146a.5.5 0 0 1 .11.54l-5.819 14.547a.75.75 0 0 1-1.329.124l-3.178-4.995L.643 7.184a.75.75 0 0 1 .124-1.33L15.314.037a.5.5 0 0 1 .54.11ZM6.636 10.07l2.761 4.338L14.13 2.576zm6.787-8.201L1.591 6.602l4.339 2.76z", Description = "", Route = "/OtpGeneration" },
    new() { Class = "IconSize OTPCustomStyle",Title = "Verify Otp", Icon ="M3 14s-1 0-1-1 1-4 6-4 6 3 6 4-1 1-1 1zm5-6a3 3 0 1 0 0-6 3 3 0 0 0 0 6", Description = "", Route = "/OtpVerification" },

};

    public class ModuleCard
    {
        public string Title { get; set; } = "";
        public string Icon { get; set; } = "";
        public string Description { get; set; } = "";
        public string Route { get; set; } = "";
        public string Class { get; set; }
    }
}

@code {
    [Inject] private NavigationManager Nav { get; set; }

    private void NavigateTo(string url) => Nav.NavigateTo(url);
}
